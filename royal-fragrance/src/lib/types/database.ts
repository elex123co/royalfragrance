/**
 * Placeholder Supabase database types.
 *
 * Once the schema in /supabase/schema.sql has been applied to your Supabase
 * project, regenerate this file with:
 *
 *   npx supabase gen types typescript --project-id <your-project-id> > src/lib/types/database.ts
 *
 * Until then this loose type keeps the Supabase client usable without
 * blocking development.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
